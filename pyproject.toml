[project]
name = "abc-dotaznik"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "kbcstorage>=0.7.0",
    "pandas>=2.2.0",
]

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
